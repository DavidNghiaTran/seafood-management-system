[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["src/**", "pom.xml", "Dockerfile"]

[deploy]
startCommand = "java -jar -Dserver.port=$PORT app.jar"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

